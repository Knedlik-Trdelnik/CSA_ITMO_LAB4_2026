trap io_handler;
грамота c = читай_память;;в порту появился символ — это событие модели
пиши_память( c ); процессор вошёл в обработчик — это реакция на событие
trap-end;

main;

цело a = -1;
for ( цело i = 5 , i < 10 , i = i + 1 );
a = a + 1;
for-end;
пиши_память( a );
main-end;