trap io_handler;
грамота c = читай_память( 800 ); в порту появился символ — это событие модели
пиши_память( 804 , c ); процессор вошёл в обработчик — это реакция на событие
trap-end;

main;
цело a = 0;
for ( цело i = 0 , i < 10 , i = i + 1 );
a = a + 1;
for-end
main-end;